import 'package:flutter/material.dart';

class BrowserTile extends StatelessWidget {
  const BrowserTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
