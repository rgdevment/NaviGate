import 'package:flutter/material.dart';

class PickerWindow extends StatelessWidget {
  const PickerWindow({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
