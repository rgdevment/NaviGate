name: linkunbound_core
description: Core business logic for LinkUnbound browser picker. Pure Dart, no Flutter dependencies.
version: 1.0.0
repository: https://github.com/rgdevment/LinkUnbound
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  json_annotation: ^4.9.0
  logging: ^1.3.0
  path: ^1.9.0

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.9.0
  lints: ^6.0.0
  test: ^1.25.0
